# data/samples

Intended home for real (or realistic synthetic) product listing captures
used for evaluation. **Empty in V1** — see `docs/dataset.md` for why no
fabricated sample data was placed here, and see
`apps/backend/tests/fixtures/html/` for the six HTML fixtures actually used
by the automated test suite today.
