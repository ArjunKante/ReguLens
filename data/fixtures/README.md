# data/fixtures

Intended home for OCR/image evaluation fixtures beyond the six HTML
scraper fixtures already under `apps/backend/tests/fixtures/html/` (which
are used by the automated pytest suite). **Empty in V1** — see
`docs/dataset.md`.
