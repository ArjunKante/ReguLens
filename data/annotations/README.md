# data/annotations

Intended home for human-labeled ground truth (the correct declaration value
per field for each sample in `data/samples/`), used to compute real
precision/recall/F1 for declaration extraction, classification, and
compliance status. **Empty in V1** — see `docs/dataset.md`.
